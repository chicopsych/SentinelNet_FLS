# ─── Dashboard Web (Flask) ──────────────────────────────────────────────────
# Flask: micro-framework web para a API de observabilidade e o dashboard.
# Werkzeug (dependência do Flask) fornece utilitários HTTP e WSGI.
flask>=3.0

# ─── Validação de Dados / Schemas ────────────────────────────────────────────
# Pydantic v2: validação de modelos, tipagem estática em runtime e serialização
# JSON nativa. A v2 foi reescrita em Rust (via pydantic-core) e tem uma API
# distinta da v1 — exige ConfigDict em vez do bloco "class Config" legado.
pydantic>=2.0

# ─── Conectividade SSH Multi-Vendor ──────────────────────────────────────────
# Netmiko: biblioteca de SSH construída sobre Paramiko, porém com suporte a
# dezenas de fabricantes (MikroTik, Cisco, Juniper, etc.). Abstrai diferenças
# de prompt, paginação e autenticação.
netmiko>=4.0

# ─── Parsing de Saída CLI ────────────────────────────────────────────────────
# TTP (Template Text Parser): converte texto semi-estruturado de CLIs
# proprietárias em objetos Python/JSON usando templates declarativos.
# Alternativa ao TextFSM com sintaxe mais próxima do Jinja2.
ttp>=0.9

# ─── Criptografia de Credenciais (Task 07) ───────────────────────────────────
# Fernet: criptografia simétrica (AES-128-CBC + HMAC-SHA256) para armazenar
# senhas de dispositivos de rede em repouso. A Master Key fica em variável
# de ambiente (SENTINEL_MASTER_KEY), nunca em arquivo.
cryptography>=41.0

# ─── Geração de Relatórios HTML (Task 06) ────────────────────────────────────
# Jinja2: motor de templates para renderizar relatórios de auditoria em HTML.
# Usado pelo ReportManager para gerar relatórios visuais entregáveis ao
# cliente. Possui fallback para template string se não estiver instalado.
jinja2>=3.1

# ─── Geração de Relatórios HTML (Task 06) ────────────────────────────────────
# Jinja2: motor de templates para renderizar relatórios de auditoria em HTML.
# Usado pelo ReportManager para gerar relatórios visuais entregáveis ao
# cliente. Possui fallback para template string se não estiver instalado.
jinja2>=3.1
