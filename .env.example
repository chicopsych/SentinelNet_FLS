# .env.example
# ════════════════════════════════════════════════════════════════════════════════
# Exemplo de arquivo .env para desenvolvimento local
#
# INSTRUÇÕES:
#   1. Copie este arquivo para .env:
#        cp .env.example .env
#
#   2. Gere uma NOVA Master Key (nunca use a dos outros desenvolvedores):
#        python -m utils.vault_setup generate-key
#
#   3. Cole a chave gerada abaixo:
#
#   4. Gere um token para testes (opcional):
#        python3 -c \"import secrets; print(secrets.token_urlsafe(32))\"
#
# ⚠️  IMPORTANTE:
#   - .env está no .gitignore — NUNCA será versionado
#   - Em produção, injete estas variáveis no ambiente do SO (systemd, Docker, etc.)
#   - NUNCA compartilhe ou versione a chave real!
# ════════════════════════════════════════════════════════════════════════════════\n\n# Master Key para o cofre de credenciais criptografado\n# Gerada com: python -m utils.vault_setup generate-key\n# ⚠️  Use uma chave ÚNICA para cada ambiente (dev, staging, prod)\nSENTINEL_MASTER_KEY=COLE_SUA_CHAVE_AQUI_OU_DEIXE_VAZIO_PRA_GERAR\n\n# Token estático para autenticação de API (endpoints /admin/*, /remediation/api/*)\n# Gerado com: python3 -c \"import secrets; print(secrets.token_urlsafe(32))\"\n# Deixe vazio para desabilitar (desenvolvimento sem autenticação)\nAPI_STATIC_TOKEN=seu_token_de_teste_aqui\n\n# Configuração do Flask\nFLASK_ENV=development\nFLASK_HOST=127.0.0.1\nFLASK_PORT=5000\nFLASK_DEBUG=1\n\n# (Opcional) Timeout para operações nmap / discovery\nDISCOVERY_TIMEOUT=120\n\n# (Opcional) Tamanho máximo de upload de arquivo\nMAX_UPLOAD_SIZE=10485760  # 10 MB em bytes\n"